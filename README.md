# profile card generator
A simple full-stack application where users can enter their name, bio, and profile image URL to generate a personalized profile card. The project demonstrates handling form submissions, POST requests, backend processing, and dynamic data display on the frontend.
